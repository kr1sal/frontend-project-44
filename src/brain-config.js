const difficulty = {
    easy: 10,
    normal: 50,
    hard: 100,
    insane: 1000,
};

const defaultDifficultyMode = difficulty.easy;

const defaultRoundsCount = 3;

const defaultUserName = 'Anon';

export { defaultUserName, defaultDifficultyMode, defaultRoundsCount };